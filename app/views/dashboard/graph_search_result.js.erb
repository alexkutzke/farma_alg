$("#search-result").html("<%= escape_javascript(render(:partial => 'graph_search_result', :locals => {:as => @as}))%>");

$("#search-result-container").show();

var i=0;

$('.answer').each(function() {
	$(this).hide();
});

$('.answer').each(function() {
	$(this).delay((i++)*50).fadeIn();
});