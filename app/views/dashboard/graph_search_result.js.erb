
$("#graph-search-result").html("<%= escape_javascript(render(:partial => 'graph_search_result', :locals => {:as => @as}))%>");

$("#search-result-container").show();

var i=0;

$('.answer').each(function() {
	$(this).hide();
});

$('.answer').each(function() {
  $(this).delay((i++)*50).fadeIn();
});

$("tr.answer").click(function(e){
  addAnswer($(this).data("answer-id"));
});

$("#add-all").click(function(){
  $("tr.answer").each(function(e){
    addAnswer($(this).data("answer-id")); 
  });
});